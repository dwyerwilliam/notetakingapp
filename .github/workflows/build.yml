name: Build & Publish

on:
  push:
    branches: [ primary ]
    tags:
      - 'v*'
  pull_request:
    branches: [ primary ]

env:
  DOTNET_VERSION: '9.0.x'
  PROJECT_PATH: 'NotePad/NotePad.csproj'

jobs:
  build:
    name: Build (.NET 9.0)
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --no-restore --configuration Release

      - name: Run tests
        run: dotnet test --no-build --configuration Release

  publish:
    name: Publish Windows x64
    needs: build
    runs-on: windows-latest
    if: github.event_name == 'push' && (github.ref == 'refs/heads/primary' || startsWith(github.ref, 'refs/tags/'))
    permissions:
      contents: write
    env:
      VERSION: '1.0.0'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore dependencies
        run: dotnet restore

      - name: Set version
        shell: pwsh
        run: |
          if ($env:GITHUB_REF -match '^refs/tags/v') {
            $version = $env:GITHUB_REF_NAME -replace '^v', ''
            "VERSION=$version" >> $env:GITHUB_ENV
          }

      - name: Publish self-contained
        run: |
          dotnet publish ${{ env.PROJECT_PATH }} `
            --configuration Release `
            --runtime win-x64 `
            --self-contained true `
            -p:PublishSingleFile=true `
            -p:IncludeAllContentForSelfExtract=true `
            -p:AssemblyVersion=${{ env.VERSION }} `
            -p:FileVersion=${{ env.VERSION }} `
            -o ./publish

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: NotePad-win-x64
          path: ./publish/
          retention-days: 30

      - name: Zip release
        run: Compress-Archive -Path ./publish/* -DestinationPath ./NotePad-${{ env.VERSION }}.zip

      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: ./NotePad-${{ env.VERSION }}.zip
          generate_release_notes: true
          draft: false
          prerelease: false
